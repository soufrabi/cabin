{
  "name": "cabin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && mkdir -p dist && cp -r src/* dist/",
    "deploy": "gh-pages --no-history -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
